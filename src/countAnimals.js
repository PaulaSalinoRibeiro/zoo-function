const data = require('../data/zoo_data');

function countAnimals(animal) {
  // without parameter --> map obj {specie: qta}
  // with parameter --> find qta
  // with parameter contain sex --> qta
}

module.exports = countAnimals;
